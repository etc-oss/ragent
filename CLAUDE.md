# CLAUDE.md

Claude Code reads this file automatically. To avoid duplication, the project
context lives in one place for every harness: **[AGENTS.md](AGENTS.md)**.

Read [AGENTS.md](AGENTS.md) first. It points to the knowledge bundle
(`docs/knowledge/`), the conventions, the architecture overview, and the forward
plan. Everything an agent needs to work in this repo is reachable from there.

Quick reminders (full detail in AGENTS.md):

- Plan with Opus; execute volume with Sonnet (see ADR-0009).
- Record non-trivial decisions as ADRs and link them to the originating session.
- Session records are verbatim — never paraphrase them.
- Reference upstreams as pinned flake inputs; never vendor their source.
- `docs/html/` is generated by `tools/okf_render.py`; never hand-edit it.
- Do not add a remote, push, or make the repo public without explicit human
  confirmation.
